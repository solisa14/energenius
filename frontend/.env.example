# Supabase (browser-safe publishable key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# FastAPI (see backend; default local)
VITE_API_URL=http://localhost:8000

# Set to "true" only to use bundled UI mocks with no running API
# VITE_USE_MOCKS=true
